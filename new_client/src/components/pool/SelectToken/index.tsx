import React, { useState } from "react";

import {
    TokenWrapper,
} from "./styles";

export const SelectToken: React.FC = () => {
    
    return (
        <TokenWrapper>

        </TokenWrapper>
    );
}